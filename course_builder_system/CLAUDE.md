# CLAUDE.md

Claude Code compatibility note: the canonical shared agent instructions for
this repository now live in `AGENTS.md`.

Before working in this repo, read `AGENTS.md` from the same directory. It has
the current prototype-complete status, run commands, architecture invariants,
branch naming convention, and collaboration preferences.

`CLAUDE.local.md` may contain private per-user preferences. Do not copy secrets
from local-only files into tracked docs.
